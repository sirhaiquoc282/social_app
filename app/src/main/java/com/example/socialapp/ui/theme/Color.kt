package com.example.socialapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette: Navy Blue ──────────────────────────────────────────────
val NavyBlue         = Color(0xFF1B2A6B)   // Xanh Navy chủ đạo
val NavyBlueDark     = Color(0xFF0D1B4A)   // Navy tối (top bar, header)
val NavyBlueLight    = Color(0xFF2E3F8F)   // Navy nhạt hơn

// ── Accent: Sky Blue ─────────────────────────────────────────────────────────
val LightSkyBlue     = Color(0xFFADD8FF)   // Xanh da trời nhạt (bubble gửi, accent)
val SkyBlue          = Color(0xFF5BA4CF)   // Xanh da trời trung
val SkyBlueDeep      = Color(0xFF2196F3)   // FAB, buttons

// ── Neutral ──────────────────────────────────────────────────────────────────
val White            = Color(0xFFFFFFFF)
val OffWhite         = Color(0xFFF0F4FF)   // Background nhạt
val LightGray        = Color(0xFFE8EDF5)   // Divider, input background
val MediumGray       = Color(0xFF9BA4B5)   // Hint text
val DarkGray         = Color(0xFF3D4563)   // Secondary text

// ── Status colors ─────────────────────────────────────────────────────────────
val OnlineGreen      = Color(0xFF4CAF50)
val ErrorRed         = Color(0xFFE53935)
val WarningAmber     = Color(0xFFFF8F00)

// ── Call Screen ───────────────────────────────────────────────────────────────
val CallBgDark       = Color(0xFF0A1628)   // Nền màn hình call
val EndCallRed       = Color(0xFFE53935)
val AcceptCallGreen  = Color(0xFF43A047)
val MuteButtonColor  = Color(0xFF37474F)

